<header>
    <img class="banner" src="/img/banner.png" alt="">
    <div class="logo">
        <h1>Travel</br>Bucket List</h1>
    </div>
    <div class="links">
        <a class="{{#if (isEqual active 'index')}} active {{/if}}" href="/">home</a>
        <a class="{{#if (isEqual active 'about')}} active {{/if}}" href="/about">about</a>
        <a class="{{#if (isEqual active 'addLocation')}} active {{/if}}" href="/addLocation">add location</a>
    </div>
</header>